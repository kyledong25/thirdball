services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: thirdball
      POSTGRES_USER: thirdball
      POSTGRES_PASSWORD: thirdball_dev
    ports:
      - "5432:5432"
    volumes:
      - thirdball-postgres:/var/lib/postgresql/data

volumes:
  thirdball-postgres:
