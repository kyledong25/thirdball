# Local development uses Vite's /api proxy.
VITE_API_BASE_URL=/api

# On Vercel, set this environment variable in the project settings before the
# production build. It must include the Spring Boot API path and no credential.
# VITE_API_BASE_URL=https://your-render-service.onrender.com/api
